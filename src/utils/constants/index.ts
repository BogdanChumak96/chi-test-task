export enum ModalVariants {
  EDIT = "edit",
  DELETE = "delete",
  ADD = "add",
}

export enum IsAble {
  AVAILABLE = "Available",
  NOTAVAILABLE = "Not Available",
}

export enum isActiveButton {
  ACTIVE = "active",
  DISABLE = "disabled",
}
